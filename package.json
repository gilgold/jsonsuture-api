{
  "name": "jsonsuture-api",
  "version": "1.0.0",
  "private": false,
  "description": "Deterministic JSON repair and schema validation for AI agents",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "vitest run --config vitest.config.mjs",
    "test:watch": "vitest --config vitest.config.mjs",
    "benchmark": "vite-node scripts/generate-benchmark.ts"
  },
  "dependencies": {
    "jsonrepair": "3.15.0",
    "jsonschema": "1.5.0"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "5.9.2",
    "vitest": "3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
